{
  "schema_registry": {
    "current_version": "2025-11-27",
    "versions": [
      {
        "version": "2025-11-27",
        "release_date": "2025-11-27",
        "description": "Initial protocol version with unified question format and multi-agent conversation schemas",
        "implementation": {
          "schemas": "2025-11-27/schemas.ts",
          "validator": "2025-11-27/validate.ts",
          "test": "2025-11-27/test-validation.ts",
          "json_schema_generator": "2025-11-27/generate-json-schemas.ts"
        },
        "validation": {
          "source_of_truth": "Zod schemas in schemas.ts",
          "runtime_validation": "validate.ts CLI called by Python via subprocess",
          "json_schemas": "Optional documentation files (*.schema.json) generated by generate-json-schemas.ts"
        },
        "breaking_changes": [],
        "notes": [
          "Unified question format supporting BBQ, DiscrimEval, and DifferenceAwareness benchmarks",
          "Flexible agent configuration with role, persona, demographics, and if_as_human attributes",
          "Round-based conversation structure with configurable routing strategies",
          "Comprehensive metadata tracking for reproducibility",
          "Zod schemas (schemas.ts) as single source of truth for validation",
          "TypeScript types automatically inferred from Zod schemas",
          "Runtime validation via validate.ts CLI (called by Python)",
          "JSON Schema files (*.schema.json) are optional and auto-generated for documentation only"
        ]
      }
    ]
  },
  "documentation": {
    "overview": "This registry tracks all protocol versions for the multi-agent conversation fairness evaluation framework",
    "naming_convention": "Schema directories follow MCP convention (YYYY-MM-DD)",
    "versioning_policy": "New versions are created when schemas have breaking changes or significant additions",
    "backward_compatibility": "Older transcripts remain valid and parseable with their specified protocol_version",
    "validation_workflow": "Python → subprocess → validate.ts → schemas.ts (Zod) → validation result"
  }
}